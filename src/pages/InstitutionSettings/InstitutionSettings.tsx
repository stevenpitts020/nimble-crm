import React from 'react'
import { InstitutionSettings } from '../../components'

const InstitutionSettingsPage: React.FC = (props: any) => {
  return <InstitutionSettings />
}

export default InstitutionSettingsPage
